#include <iostream>
#include "math.h"

using namespace std;

int main()
{
    int a(4);
    float b,c,e;
    b = 12.25;
    c = 0.0121;
    cout << sqrt(a)<<endl << sqrt(b)<<endl << sqrt(c) << endl;
    cin >> e;
    cout << "Square root = "<< sqrt(e) <<endl;

    return 0;
}